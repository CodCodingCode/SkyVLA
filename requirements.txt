# Python dependencies for drone_project (OpenFly outdoor VLN track).
# `bash openfly/setup.sh` installs these into the `openfly` conda env and also
# pulls extras (airsim, msgpack-rpc-python, flash-attn) that are awkward via
# requirements.txt. This file is the canonical "what does the repo import?"
# reference.

torch>=2.2
torchvision>=0.17
transformers>=4.45,<5
huggingface_hub>=0.24
accelerate>=0.30
timm>=1.0
einops>=0.7
numpy>=1.26
pillow>=10
opencv-python>=4.9
pyyaml>=6
tqdm>=4.66
tensorboard>=2.15
wandb>=0.17
gymnasium>=1.0


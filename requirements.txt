# Python dependencies for skyvla_isaac (Isaac Sim gripper-drone work).
#
# The heavy stack — Isaac Sim 4.5, Isaac Lab 2.1, rsl_rl, torch, numpy,
# gymnasium — comes from the Isaac Sim / Isaac Lab installer into the `isaac`
# conda env, NOT from this file. Install those per NVIDIA's instructions first.
#
# These are the only pip extras the repo adds on top of that env:

gsplat>=1.3      # 3D Gaussian-Splatting rasterizer for the navigation map (gs/)
imageio>=2.34    # mp4 encoding for render_rollout.py / play.py --video
